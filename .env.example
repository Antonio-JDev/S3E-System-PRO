# ============================================
# S3E System PRO - Variáveis de Ambiente
# ============================================
# Copie este arquivo para .env e ajuste os valores conforme necessário
# Para desenvolvimento local: cp .env.example .env
# Para produção: NUNCA commite o arquivo .env.production
# ============================================

# ============================================
# AMBIENTE
# ============================================
NODE_ENV=development
# Opções: development, production

# ============================================
# BANCO DE DADOS (PostgreSQL)
# ============================================
DB_USER=s3e_user
DB_PASSWORD=s3e_password
DB_NAME=s3e_db
DB_PORT=5432
DATABASE_URL=postgresql://s3e_user:s3e_password@postgres:5432/s3e_db?schema=public

# ============================================

# ============================================
# BACKEND API
# ============================================
BACKEND_PORT=3001
PORT=3001

# ============================================
# FRONTEND
# ============================================
FRONTEND_PORT=5173
VITE_API_URL=http://localhost:3001

# ============================================
# AUTENTICAÇÃO JWT
# ============================================
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=1d
# Opções: 1h, 1d, 7d, etc.

# ============================================
# CORS (Cross-Origin Resource Sharing)
# ============================================
CORS_ORIGIN=http://localhost:5173
# Para múltiplas origens, separe por vírgula: http://localhost:5173,http://localhost:3000
FRONTEND_URL=http://localhost:5173

# ============================================
# NGINX (Produção)
# ============================================
NGINX_HTTP_PORT=80
NGINX_HTTPS_PORT=443

# ============================================
# PGADMIN (Desenvolvimento)
# ============================================
PGADMIN_EMAIL=admin@s3e.com
PGADMIN_PASSWORD=admin
PGADMIN_PORT=5050

# ============================================
# EMAIL/SMTP (Opcional)
# ============================================
# Configuração de email para recuperação de senha e notificações
SMTP_HOST=smtps.uhserver.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=contato@s3eengenharia.com.br
SMTP_PASS=Eng.elet30838361
SMTP_FROM=contato@s3eengenharia.com.br

# ============================================
# PUPPETEER (Geração de PDFs)
# ============================================
PUPPETEER_SKIP_CHROMIUM_DOWNLOAD=true
PUPPETEER_EXECUTABLE_PATH=/usr/bin/chromium-browser

# ============================================
# NOTAS: 
# ============================================
# 1. Para desenvolvimento local, use os valores padrão acima
# 2. Para produção, crie .env.production com valores reais
# 3. NUNCA commite arquivos .env ou .env.production
# 4. O arquivo .env.production deve conter credenciais reais de produção
# 5. Use senhas fortes em produção para JWT_SECRET e DB_PASSWORD
